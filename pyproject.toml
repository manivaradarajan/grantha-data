# In pyproject.toml

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "grantha_converter"
version = "0.0.1"
# ... (rest of your project section is fine) ...
description = "A tool to convert Grantha data"
readme = "tools/lib/grantha_converter/README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

# CORRECTED SECTION
[tool.setuptools]
packages = ["grantha_converter", "tools", "tools.scripts"]
[tool.setuptools.package-dir]
grantha_converter = "tools/lib/grantha_converter"
tools = "tools"

[project.urls]
"Homepage" = "https://github.com/pypa/sampleproject"
"Bug Tracker" = "https://github.com/pypa/sampleproject/issues"

[project.scripts]
# This script mapping is now correct because both packages will be installed
grantha-converter = "grantha_converter.cli:main"
vishvas-markup-to-structured-markdown = "grantha_converter.html_details_cli:main"
grantha-repair-devanagari = "grantha_converter.devanagari_repair_cli:main"
pdf-ocr = "tools.scripts.pdf_ocr:main"